"""pockettts_coreml — Python conversion + verification harness for the
PocketTTS→CoreML port. Phase 1 only exposes the oracle subpackage; later
phases will add `convert`, `quantize`, and `bench`.
"""

__version__ = "0.0.1"
