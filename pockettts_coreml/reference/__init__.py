"""Vendored Kyutai pocket-tts repo, ingested as a git subtree.

After running `git subtree add --prefix=pockettts_coreml/reference ...`, the
upstream repo's top-level files will live in this directory. Imports should
use the fully qualified path:

    from pockettts_coreml.reference.pocket_tts.models.tts_model import TTSModel

Do not edit files inside `pocket_tts/` after subtree ingestion — those are
upstream-tracked. Our modifications live in `pockettts_coreml/patches/`
(Phase 2).
"""
